server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Kök istekleri varsayılan dile (İngilizce) yönlendir
    location = / {
        return 302 /en/;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
